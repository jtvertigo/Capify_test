{% block calendar %}
    <script>
	    $(function() {
            // $( "#datepicker" ).datepicker({ showAnim: 'drop' });
            $( "#datepicker" ).datepicker( { altField: '#actualDate' } );
	    });
	</script>
{% endblock %}

<div class="topbar">
    <div class="topbar-inner">
        <div class="container-fluid">
            <a class="brand" href="{{ path('notifies') }}">{{ JTVProjectName }}</a>
            <ul class="nav">
                <li class="active"><a href="{{ path('notifies') }}">Home</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#contact">Contact</a></li>
                <li class="dropdown" data-dropdown="dropdown" >
                    <a href="#" class="dropdown-toggle">Dropdown</a>
                    <ul class="dropdown-menu">
                        <li><a href="#">Secondary link</a></li>
                        <li><a href="#">Something else here</a></li>
                        <li class="divider"></li>
                        <li><a href="#">Another link</a></li>
                    </ul>
                </li>
            </ul>
            <form class="pull-left" action="" method="POST">
                <input type="text" id="datepicker" placeholder="Search by date" size="30"/>

                <input type="hidden" id="actualDate" type="text" />

                <input type="submit" value="Search" />
            </form>
            <p class="pull-right">Logged in as <a href="#">username</a></p>
        </div>
    </div>
</div>
